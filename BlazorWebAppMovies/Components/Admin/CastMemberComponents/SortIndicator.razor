@inject ICastMemberFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}

@code {
    [Parameter]
    public CastMemberFilterColumns Column { get; set; }

    private string SortSymbol => Filters.SortAscending ? "🔼" : "🔽";
}
